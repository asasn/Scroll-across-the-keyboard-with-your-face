<Window x:Name="winMain" x:Class="脸滚键盘.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:脸滚键盘" xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:controls="clr-namespace:HandyControl.Controls;assembly=HandyControl"
        mc:Ignorable="d" 
        Title="脸滚键盘——优秀的小说创作解决方案" Height="1000" Width="1800" Closed="Window_Closed" WindowState="Maximized">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="260" MaxWidth="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="300" MinWidth="260" MaxWidth="400"/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#FFFFFFFF"/>
        <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="#FFFFFFFF"/>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>
            <local:uc_Editor x:Name="ucEditor" Grid.Column="2" Grid.RowSpan="2" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="{Binding UcTag, ElementName=ucBookTree, Mode=TwoWay}" CurItem="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" Margin="6" Loaded="ucEditor_Loaded"/>
            <local:uc_NoteTree x:Name="ucNote" Grid.Column="4" UcTitle="时间事件备忘笔记" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="note" IsEnabled="False" Loaded="ucNote_Loaded" />
            <local:uc_NoteTree x:Name="ucTask" Grid.Column="4" Grid.Row="1" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTitle="任务清单" UcTag="story" IsEnabled="False" Loaded="ucTask_Loaded" />
            <hc:Card x:Name="InfoCard" Margin="5" Header="信息卡" Background="#FFFAFAFA" Grid.Row="1" Effect="{StaticResource EffectShadow3}" UseLayoutRounding="True" Loaded="InfoCard_Loaded" >
                <hc:TabControl Style="{StaticResource TabControlInLine}" IsTabFillEnabled="True"  Grid.Row="1" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}">
                    <hc:TabItem Header="角色信息卡" Padding="2" >
                        <Grid Background="#FFE5E5E5">
                            <local:uc_InfoCard x:Name="ucRoleCard" UcTitle="角色" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="角色" Loaded="ucRoleCard_Loaded"/>
                        </Grid>
                    </hc:TabItem>
                    <hc:TabItem Header="地区和势力" Padding="2"  >
                        <Grid Background="#FFE5E5E5">
                            <local:uc_InfoCard x:Name="ucFactionCard" UcTitle="势力" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="势力" Loaded="ucFactionCard_Loaded"/>
                        </Grid>
                    </hc:TabItem>
                    <hc:TabItem Header="物品信息卡" Padding="2"  >
                        <Grid Background="#FFE5E5E5">
                            <local:uc_InfoCard x:Name="ucGoodsCard" UcTitle="物品" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="物品" Loaded="ucGoodsCard_Loaded"/>
                        </Grid>
                    </hc:TabItem>
                    <hc:TabItem Header="通用信息卡" Padding="2"  >
                        <Grid Background="#FFE5E5E5">
                            <local:uc_InfoCard x:Name="ucCommonCard" UcTitle="通用" DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="通用" Loaded="ucCommonCard_Loaded"/>
                        </Grid>
                    </hc:TabItem>
                </hc:TabControl>
            </hc:Card>
            <local:uc_Searcher DataContext="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTag="{Binding UcTag, ElementName=ucBookTree, Mode=TwoWay}" CurItem="{Binding CurItem, ElementName=ucBookTree, Mode=TwoWay}" UcTitle="文章检索" VerticalAlignment="Top" />
            <hc:Card Background="#FFFAFAFA" Header="工具箱" Margin="5,106,5,5">
                <Grid>

                    <Button x:Name="btnBookSettings" Content="书籍信息" HorizontalAlignment="Left" VerticalAlignment="Top" Click="btnBookSettings_Click"/>

                </Grid>
            </hc:Card>
        </Grid>
        <local:uc_BookTree x:Name="ucBookTree" UcTitle="书籍目录" UcTag="books" Loaded="ucBookTree_Loaded"/>
        <local:uc_MaterialTree x:Name="ucMaterialTree" Grid.Column="4" UcTitle="资料库" UcTag="material" Loaded="ucMaterialTree_Loaded"/>
        <ScrollViewer Grid.Column="2" Grid.ColumnSpan="3" VerticalScrollBarVisibility="Hidden" HorizontalAlignment="Right">
            <StackPanel controls:Growl.GrowlParent="True" VerticalAlignment="Top" Margin="0,10,10,10"/>
        </ScrollViewer>
        <hc:Drawer Name="DrawerBottomInContainer" Dock="Bottom" ShowMask="False">
            <Border Background="{DynamicResource RegionBrush}" Height="400" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid Margin="10,0,0,0" Grid.Row="0">
                        <TextBlock x:Name="drawerTbk" Text="当前书籍：" Style="{StaticResource TextBlockTitle}" HorizontalAlignment="Left"/>
                        <Button HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryTextBrush}" Style="{StaticResource ButtonIcon}" hc:IconElement.Geometry="{StaticResource DeleteFillCircleGeometry}" Click="btnBookSettings_Click"/>
                    </Grid>
                    <Grid Margin="10,0,0,0" Grid.Row="1">
                        <TextBox x:Name="tbPrice" Padding="2" hc:InfoElement.Title="稿费（元/千字）" hc:InfoElement.TitlePlacement="Left" Style="{StaticResource TextBoxExtend}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="150" TextChanged="tbPrice_TextChanged" Loaded="tbPrice_Loaded" Margin="5" />
                        <TextBox x:Name="tbBornYear" Padding="2" hc:InfoElement.Title="主角出生年份" hc:InfoElement.TitlePlacement="Left" Style="{StaticResource TextBoxExtend}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="150" TextChanged="tbBornYear_TextChanged"  Loaded="tbBornYear_Loaded" Margin="5,40,5,5" />
                        <TextBox x:Name="tbCurYear" Padding="2" hc:InfoElement.Title="当前剧情年份" hc:InfoElement.TitlePlacement="Left" Style="{StaticResource TextBoxExtend}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="150" TextChanged="tbCurYear_TextChanged" Loaded="tbCurYear_Loaded" Margin="5,80,5,5" />

                    </Grid>
                </Grid>
            </Border>
        </hc:Drawer>
    </Grid>
</Window>
